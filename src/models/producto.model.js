import pool from "../config/db.js";

export class productoModelo {
    static async crear() {
        try {
            
        } catch (error) {
            return { success: false, error }
        }
    }
}